Drop widgets here, while core code will be updated separately, this folder will be skipped.

To use a completely different folder, create a configure.php file and define your plugins path

define('MGM_EXTEND_WIDGET_DIR', 'Your Dir');

or use wordpress wp-config.php